\ProvidesPackage{setup}

\newenvironment{SongText}[1][] %Setup up at the begining of a song
{\filbreak
\subsection{\textbf{#1}}}
{\filbreak}

\newenvironment{SongVerse} %Setup up at the begining of a verse
{}
{\filbreak}

\newenvironment{SongInfo}
{}
{
\\*
\phantom{NULL}
\\*
}

\newenvironment{Censor}[1]
{
\ifprinting

\else
#1
\fi
}

\newenvironment{PrintingVariant}[2]
{
\ifprinting
#1
\else
#2
\fi
}

\usepackage{graphicx} % Required for inserting images
%\usepackage{parskip} % Stops indenet after paragraph change
\usepackage{fancyhdr}
\usepackage{ragged2e} 
\raggedbottom
\sloppy % Tries to force lines to stay within margins
\usepackage{titling}
\newcommand{\subtitle}[1]{%
  \posttitle{%
    \par\end{center}
    \begin{center}\large#1\end{center}
    \vskip0.5em}%
}
\usepackage{tabularx}
\usepackage{multicol} %%Används för listan under snapsvisor

\usepackage{silence} % Temporary solution
\WarningsOff[everypage]% Suppress warnings related to package everypage
\usepackage{background} %Uses hooks from The "everypage" package which is outdated

\usepackage{setspace}
\linespread{1,1} % Increases the spacing between lines by 10%

\newcommand{\CustomBackground}[1]{
    \backgroundsetup{
        scale=1,
        opacity=1,
        angle=0,
        color=black,
        contents={%
        \ifodd\value{page}
            \includegraphics[width=\paperwidth,height=\paperheight]{Media/#1}%%Odd pages
        \else
            \includegraphics[width=\paperwidth,height=\paperheight, angle=180]{Media/#1}%%Even pages
        \fi
        }
    }
}

\newcommand{\CustomPrintingBackground}[1]{%Used to add the 3mm shift at the bottom of the background when using the printing version 
    \backgroundsetup{
        scale=1,
        opacity=1,
        angle=0,
        color=black,
        vshift=-3mm, %This one is the fix
        hshift=-3mm,
        contents={
        \ifodd\value{page}
          \includegraphics[width=\paperwidth,height=\paperheight,angle=180]{Media/#1}%%Odd pages
        \else
          \includegraphics[width=\paperwidth,height=\paperheight]{Media/#1}%%Even pages
        \fi
        }
    }
}



% Används att alla section headers ska fungera som vanligt men inte skrivas ut
\newcommand{\fakesection}[1]{%
  \par\refstepcounter{section}% Increase section counter
  \sectionmark{#1}% Add section mark (header)
  \addcontentsline{toc}{section}{\protect\numberline{\thesection}#1}% Add section to ToC
  \setlength{\headheight}{12.96115pt}%Capital Ö is 12.96115pt 
  % Add more content here, if needed.
}
% Används där alla sections ska vara osynliga och inte läggas till i toc
\newcommand{\invisiblesection}[1]{%
  \par\refstepcounter{section}% Increase section counter
  \sectionmark{#1}% Add section mark (header)
  %\addcontentsline{toc}{section}{\protect\numberline{\thesection}#1}% Add section to ToC
  % Add more content here, if needed.
}
\newcommand{\invisiblesubsection}[1]{%
  \par\refstepcounter{subsection}% Increase section counter
  \subsectionmark{#1}% Add section mark (header)
  %\addcontentsline{toc}{section}{\protect\numberline{\thesection}#1}% Add section to ToC
  % Add more content here, if needed.
}
\newcommand{\invisiblesubsubsection}[1]{%
  \par\refstepcounter{subsubsection}% Increase section counter
  \subsectionmark{#1}% Add subsection mark (header)
  %\addcontentsline{toc}{section}{\protect\numberline{\thesection}#1}% Add section to ToC
  % Add more content here, if needed.
}

\renewcommand{\thesection}{} % Removes the numbering from the left side of sections in the ToC

\renewcommand{\thesubsection}{} % Removes the numbering from the left side of subsections in the ToC

\renewcommand{\thesubsubsection}{} % Removes the numbering from the left side of subsections in the ToC

\usepackage{geometry} % Switch on for non frame view
%\usepackage[showframe]{geometry} % Switch on for frame view
 \geometry{a6paper}

 \usepackage{lmodern}
 \usepackage{layouts} % Used for debuging the goemetry sizes

 % New Margins and shit (EXPERIMENTAL AS OF 2024-10-28)
 
 \setlength{\textheight}{125mm}
 \setlength{\headsep}{5mm}
 \setlength{\footskip}{5mm}
 


\usepackage{tocloft}
\usepackage{etoc} % Adds the functionallity to have two ToC
\setcounter{tocdepth}{3}  

\renewcommand\listoftables{%
        \@starttoc{lot}%
}  

%\usepackage{tocloft} Moved to before etoc
\setlength{\cftsecindent}{0pt}% Remove indent for \section
\setlength{\cftsubsecindent}{0pt}% Remove indent for \subsection
\setlength{\cftsubsubsecindent}{0pt}% Remove indent for \subsubsection

\usepackage{currfile}
\usepackage{pythontex}

\usepackage{adjustbox}%Used for rotating in Inledning

\usepackage{pdfpages}%Used for title pages

\usepackage{xcolor}%Used for title colour

\usepackage{longtable}%Used in O Gamla Klang och Jubeltid


\endinput